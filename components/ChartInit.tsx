/**
 * ChartInit. no-op stub kept for import compatibility.
 * Charts are now initialized directly in VendorTab via typed
 * React components (NpsChart, RepChart, CostChart, DriversChart).
 * Chart.js is loaded via the CDN Script tag in app/layout.tsx.
 */
export function ChartInit() {
  return null;
}
